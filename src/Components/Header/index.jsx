import {Menu} from "../Shared"
const Header = () => {
  return (
    <div className={`text-white border border-yellow-700 h-[70px] flex `}>
      <Menu />
      Im the header
    </div>
  )
}

export default Header;
